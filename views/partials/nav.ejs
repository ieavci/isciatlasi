<link rel="stylesheet" href="/static/css/navbar.style.css">
<nav class="navbar navbar-expand-lg navbar-dark py-3 fixed-top navbar.style.css">
    <div class="container">
        <a href="/" class="navbar-brand d-flex justify-content-between align-items-center order-lg-0">
            <img src="/static/img/atlas.png" alt="">
            <span class="text-lowercase fw-bold ms-2">isciatlasi.org</span>
        </a>

        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
            <span class="navbar-toggler-icon">
                <i class="fas fa-bars custom-icon-size"></i>
            </span>
        </button>

        <div class="collapse navbar-collapse order-lg-1" id="navMenu">
            <ul class="navbar-nav mx-auto text-center">
                <li class="nav-item px-2 py-2">
                    <a href="/" class="nav-link text-uppercase ">anasayfa</a>
                </li>
                <li class="nav-item px-2 py-2">
                    <a href="/prediction" class="nav-link text-uppercase ">öngörü</a>
                </li>
                
                <li class="nav-item px-2 py-2">
                    <a href="/sigortasiz" class="nav-link text-uppercase">Sİgortasız İşçİ</a>
                </li>
                <li class="nav-item px-2 py-2">
                    <a href="haklar" class="nav-link text-uppercase">İşçİ & İşveren Hakları</a>
                </li>

                <li class="nav-item px-2 py-2">
                    <a href="/sendikalar" class="nav-link text-uppercase">Sendİkalar</a>
                </li>
                <li class="nav-item px-2 py-2">
                    <a href="/haberler" class="nav-link text-uppercase">Haberler</a>
                </li>

            </ul>
        </div>
    </div>
</nav>
<script>$(document).ready(function () {
        var lastScrollTop = 0;
        var delta = 5;
        var navbarHeight = $('.navbar').outerHeight();

        $(window).scroll(function (event) {
            var st = $(this).scrollTop();

            // Scroll aşağı doğru yapılıyorsa ve mesafe delta'dan büyükse navbar kaybolur
            if (Math.abs(lastScrollTop - st) <= delta)
                return;

            if (st > lastScrollTop && st > navbarHeight) {
                // Scroll aşağı doğru yapılıyor
                $('.navbar').removeClass('nav-down').addClass('nav-up');
            } else {
                // Scroll yukarı doğru yapılıyor
                if (st + $(window).height() < $(document).height()) {
                    $('.navbar').removeClass('nav-up').addClass('nav-down');
                }
            }

            lastScrollTop = st;
        });
    });</script>